# ThreadsOS

<important>
## ハイブリッド委譲ルール（Codex + Gemini）【最優先・強制・hookで強制。違反時はユーザーが検知する】

ユーザーのプロンプトを受け取ったら、コード修正やファイル読み込みの**前に**、
`/delegate-check` スキルを実行して委譲判定を行うこと。

hookの `<user-prompt-submit-hook>` メッセージでも同じ指示が届く。

**これは任意ルールではない。条件に当てはまる作業を自分でやり始めたら、それは違反。**

### 大原則
Claudeのトークンは最も高コスト。**迷ったら振る。自分でやるのは最終手段。**

### 🧠 Codex（GPT-5.4）= アドバイザー・相談役

Codexは作業員ではない。頭脳として使う。

| 用途 | 具体例 |
|---|---|
| 設計のセカンドオピニオン | 「この方針で合ってる？」の壁打ち |
| アーキテクチャレビュー | 構成の妥当性チェック |
| Ops・インフラの判断アドバイス | デプロイ戦略、パフォーマンス相談 |
| 技術選定の比較検討 | フレームワーク・ライブラリの選定支援 |
| コードレビューの第三者視点 | 設計品質のダブルチェック |

### 🔨 Gemini = 作業員（雑用全般）

手を動かす作業は全部Gemini。旧Codex作業もすべてここに統合。

| 条件 | 具体例 |
|---|---|
| 触るファイルが 3 つ以上 | 一括 rename、型追加、import 整理 |
| リポジトリ全体を読む・探す | TODO 洗い出し、grep 全探索、依存関係調査 |
| 同じ作業を繰り返す | テスト修正ループ、フォーマット統一 |
| 広範囲レビュー | PR 全体レビュー、コード品質チェック |
| 機械的な変換 | JSDoc 付与、コメント翻訳、コード整形 |
| ウェブ検索・最新情報の調査 | ライブラリの最新版調査、トレンド確認 |
| 長文ドキュメントの要約・分析 | README分析、仕様書の要約 |
| API仕様・外部サービスの調査 | 公式ドキュメントの確認・比較 |
| ベストプラクティス調査 | フレームワークの推奨パターン調査 |
| コマンド実行の運用作業 | ビルド、lint、テスト実行 |
| 1ファイルでも修正箇所が6箇所以上 | 繰り返し修正 |

### 🎯 Claude が直接やる作業（これだけ）

以下の **すべて** に該当する場合のみ自分でやる:
- プロダクト設計の判断 or ユーザーとの対話が必要
- セキュリティ・機密情報を扱う
- 作業量が本当に小さい（1〜2ファイル、かつ修正箇所が5箇所以内）

### 積極委譲ルール
- **「自分でやった方が早い」は禁止思考**
- **迷ったらGemini。** 判断に3秒以上かかったらGeminiに投げろ
- **コマンド実行・ファイル読み込みだけの作業もGemini**
- 条件が競合したら → **常に委譲を優先**

判定の結果「自分でやる」になった場合のみ、コードに触ってよい。
</important>

---

### Codex 呼び出し方

```powershell
& "C:\Users\i0swi\OneDrive\デスクトップ\claude.alibaba\scripts\codex-delegate.ps1" `
    -Task "タスク内容" `
    -Mode research `
    -Targets @("対象パス") `
    -Constraints @("制約")
```

### Gemini 呼び出し方

```powershell
& "C:\Users\i0swi\OneDrive\デスクトップ\claude.alibaba\scripts\gemini-delegate.ps1" `
    -Task "タスク内容" `
    -Mode research `
    -Targets @("対象パス") `
    -Constraints @("制約")
```

戻り値 JSON（共通）: `{ ok, summary, changed_files, verification, risks, raw_output }`
