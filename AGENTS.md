# ThreadsOS

<important>
## ハイブリッド委譲ルール（Codex + Gemini）【最優先・強制・hookで強制】

ユーザーのプロンプトを受け取ったら、コード修正やファイル読み込みの**前に**、
`/delegate-check` スキルを実行して委譲判定を行うこと。

### 絶対に Codex に投げる作業（自分でやるな）

| 条件 | 具体例 |
|---|---|
| 触るファイルが 5 つ以上 | 一括 rename、型追加、import 整理 |
| リポジトリ全体を読む・探す | TODO 洗い出し、grep 全探索、依存関係調査 |
| 同じ作業を繰り返す | テスト修正ループ、フォーマット統一 |
| 広範囲レビュー | PR 全体レビュー、コード品質チェック |
| 機械的な変換 | JSDoc 付与、コメント翻訳、コード整形 |

### 絶対に Gemini に投げる作業（自分でやるな）

| 条件 | 具体例 |
|---|---|
| ウェブ検索・最新情報の調査 | ライブラリの最新版調査、トレンド確認 |
| 長文ドキュメントの要約・分析 | README分析、仕様書の要約 |
| API仕様・外部サービスの調査 | 公式ドキュメントの確認・比較 |
| ベストプラクティス調査 | フレームワークの推奨パターン調査 |

### Codex が直接やる作業（投げるな）

| 条件 | 理由 |
|---|---|
| プロダクト・設計の判断 | 文脈と意思決定が必要 |
| 曖昧な仕様の整理・確認 | ユーザーとの対話が必要 |
| セキュリティ・認証・機密情報 | 外部に渡してはいけない |
| 1〜2 ファイルの小さな修正 | オーバーヘッドの方が大きい |

### 条件が競合したときのルール

「自分でやる」条件と「Codex/Gemini行き」条件が同時に当てはまる場合 → **委譲を優先する**。

判断基準は**ファイル数ではなく作業の性質**。
- 1ファイルでも19箇所の繰り返し修正 → Codex行き
- 2ファイルでもウェブ調査が必要 → Gemini行き
- 「小さな修正」とは作業量が小さいことであって、ファイル数が少ないことではない

### 繰り返しの早期検知ルール
- **同じ種類の操作を2回やったら立ち止まれ**。3回目からはCodexに投げる
- 試行錯誤ループ（実行→確認→調整）が回り始めたら、**調整部分だけCodexに切り出す**
- 「もう1回だけ自分で…」は危険信号。そう思った時点で委譲を検討する

判定の結果「自分でやる」になった場合のみ、コードに触ってよい。
</important>

---

### Codex 呼び出し方

```powershell
& "C:\Users\i0swi\OneDrive\デスクトップ\Codex.alibaba\scripts\codex-delegate.ps1" `
    -Task "タスク内容" `
    -Mode research `
    -Targets @("対象パス") `
    -Constraints @("制約")
```

### Gemini 呼び出し方

```powershell
& "C:\Users\i0swi\OneDrive\デスクトップ\Codex.alibaba\scripts\gemini-delegate.ps1" `
    -Task "タスク内容" `
    -Mode research `
    -Targets @("対象パス") `
    -Constraints @("制約")
```

戻り値 JSON（共通）: `{ ok, summary, changed_files, verification, risks, raw_output }`
