# ThreadsOS 実装計画

> 生成日: 2026-04-08
> 仕様書ギャップ分析に基づく段階的実装ロードマップ

---

## Phase A: 土台整備（DB、設定、ログ、テスト基盤）

### A-1. DBスキーマ拡張
- [ ] `proposals` テーブル追加（提案モデル: title, department, agent, content, reason, evidence, expected_effect, risk, priority, status, reviewer_comment）
- [ ] `agent_states` テーブル追加（担当者状態: agent_id, department, role, current_task, status, last_updated）
- [ ] `budget_tracking` テーブル追加（予算管理: agent_id, period, token_used, api_calls, cost_estimate）
- [ ] `error_logs` テーブル追加（エラー専用: job_name, error_type, message, stack, resolved_at）
- [ ] `memory_summaries` テーブル追加（要約メモリ: scope, period_type, period_key, summary, raw_ref_ids）
- [ ] `kpi_snapshots` テーブル拡張（購入数・売上・CV率カラム追加）
- [ ] マイグレーション実行・テスト

### A-2. 設定・環境変数
- [ ] `BUDGET_DAILY_TOKEN_LIMIT` 追加
- [ ] `BUDGET_PER_HEARTBEAT_LIMIT` 追加
- [ ] `EMERGENCY_STOP_ENABLED` 追加
- [ ] `DASHBOARD_PORT` 追加
- [ ] `CACHE_TTL_MINUTES` 追加
- [ ] env.ts の envSchema 更新

### A-3. ログ基盤強化
- [ ] 構造化ログにトークン使用量を含める
- [ ] エラー専用テーブルへの自動記録ミドルウェア
- [ ] ジョブ実行ごとのコスト記録

### A-4. テスト基盤
- [ ] 各新テーブルのCRUDテスト
- [ ] 提案フローのユニットテスト骨格
- [ ] ダッシュボードAPI用のE2Eテスト骨格

**推定工数**: 2-3日
**依存**: なし

---

## Phase B: ドメインと状態（メモリ5階層、エージェント状態、提案モデル、予算管理）

### B-1. メモリ5階層の実装
- [ ] **A. 永続運用方針メモリ**: operatorProfiles 拡張（KPI目標、基本方針テキストを追加）
- [ ] **B. 部署別要約メモリ**: `memory_summaries` に部署スコープで自動書込み
  - 日次サマリー自動生成
  - 週次サマリー自動生成
  - 勝ちパターン/失敗パターンの自動抽出
- [ ] **C. イベントログ**: 既存テーブル群をログ保存層として位置づけ。要約・索引化サービス追加
- [ ] **D. 実行用ワーキングメモリ**: `WorkingMemoryBuilder` クラス作成
  - タスクごとに必要最小データを選別
  - プロンプト供給層として機能
- [ ] **E. KPIスナップショット**: channelPerformanceSnapshots の自動集計ジョブ追加

### B-2. エージェント状態管理
- [ ] `AgentStateService` 実装（agent_states CRUD）
- [ ] 各部署の係を agent として登録
  - Threads: 投稿生成係、返信生成係、エンゲージメント調査係、競合リサーチ係、リーダー
  - note: 記事生成係、エンゲージメント調査係、競合リサーチ係、リーダー
  - 外部リサーチ部署、競合リサーチ分析部署、管理・指揮系統
- [ ] ステータス管理（実行中/提案中/承認待ち/修正中/完了）

### B-3. 提案モデル
- [ ] `ProposalService` 実装
  - createProposal（必須要素: 何を/なぜ/根拠/効果/リスク/優先度）
  - reviewProposal（承認/差し戻し/検討中）
  - listPendingProposals
  - getProposalsByDepartment
- [ ] 提案→承認→実行のワークフロー
- [ ] 自動承認ルール（定型投稿、ルール内返信等）
- [ ] 承認必須ルール（大方針変更、大幅価格変更等）

### B-4. 予算管理
- [ ] `BudgetService` 実装
  - recordUsage（エージェント/ジョブごとのトークン使用量記録）
  - checkBudget（予算残量チェック）
  - getFallbackStrategy（予算超過時のフォールバック）
- [ ] ハートビートごとの予算チェックフック
- [ ] 日次予算レポート

**推定工数**: 4-5日
**依存**: Phase A

---

## Phase C: ハートビート強化（13ステップ標準フロー、差分処理）

### C-1. 13ステップ標準フローの完全実装
現状の `hourly-heartbeat → executive → department-execution` を以下に拡張:

1. **ハートビート起動** — 既存（runner.ts）
2. **新規イベント差分を収集** — `DiffCollector` サービス新設
   - [ ] 前回ハートビート以降の新規返信
   - [ ] 前回以降の新規エンゲージメントデータ
   - [ ] 前回以降の管理者指示
   - [ ] 前回以降のエラー
3. **重要度判定** — executive サービス拡張
   - [ ] 差分データに基づく重要度スコアリング
4. **実行対象部署を選定** — 既存（executive.planCycle）
5. **各部署へ最小コンテキスト配布** — `WorkingMemoryBuilder` 連携
   - [ ] 部署ごとの最小コンテキスト組み立て
   - [ ] 永続メモリ + 部署要約 + 差分データの合成
6. **部署内担当者が処理を実行** — department-execution 拡張
7. **リーダーが部署結果を統合** — `DepartmentLeaderService` 新設
   - [ ] 各係の実行結果を統合
   - [ ] 部署サマリー生成
8. **管理・指揮系統へ提案を送る** — ProposalService 連携
9. **自動承認可能なものは実行** — 自動承認ルールエンジン
10. **要人判断は承認待ちへ送る** — humanReviewItems / proposals
11. **ログ保存** — 既存 + エラーログ拡張
12. **サマリー更新** — メモリ5階層のB/E層を更新
13. **ダッシュボード更新** — Phase E で実装

### C-2. 差分処理の体系化
- [ ] `DiffCollector` サービス: 前回ハートビート以降の全差分を収集
- [ ] `DiffRouter`: 差分の種類に応じて処理先を振り分け
- [ ] 差分ベースのコンテキスト生成（全履歴送信の禁止を強制）

### C-3. 圧縮・要約パイプライン
- [ ] ハートビート完了後の自動要約生成
- [ ] 古い要約の再要約（週次→月次圧縮）
- [ ] 重要決定の長期メモリ昇格ルール

**推定工数**: 4-5日
**依存**: Phase B

---

## Phase D: 各部署実装（5部署+係）

### D-1. 管理・指揮系統の強化
- [ ] executive サービスに全体最適ロジック追加
  - 部署間の優先度バランス調整
  - 中長期方針の自動見直し
  - コスト配分の最適化
- [ ] 緊急停止機構
- [ ] 方針変更の自律判断ロジック

### D-2. 外部リサーチ部署の独立化
- [ ] `ExternalResearchDepartment` として構造化
- [ ] 定期的なトレンド収集スケジュール
- [ ] リサーチ結果の他部署共有メカニズム
- [ ] 有益示唆の自動抽出・分類

### D-3. 競合リサーチ分析部署の独立化
- [ ] `CompetitorAnalysisDepartment` として構造化
- [ ] 勝ちパターン/負けパターンの自動抽出
- [ ] 各部署への改善提案の自動生成

### D-4. Threads運用部署の係分離
- [ ] 投稿生成係: post-generation をラップ
- [ ] 返信生成係: reply-execution をラップ
- [ ] エンゲージメント調査係: engagement-analysis をラップ
- [ ] 競合リサーチ係: scraper + competitor分析をラップ
- [ ] リーダー: 4係の結果統合 + 部署提案生成

### D-5. note運用部署の係分離
- [ ] 記事生成係: note-generation をラップ
- [ ] エンゲージメント調査係: note-engagement-analysis をラップ
- [ ] 競合リサーチ係: note-research をラップ
- [ ] リーダー: 3係の結果統合 + 価格調整案の作成

### D-6. 部署間連携の実装
- [ ] 部署間メッセージング（strategyStates ベース）
- [ ] Threads→note連携の強化（集客意図の共有）
- [ ] リサーチ結果の自動配布

**推定工数**: 5-7日
**依存**: Phase C

---

## Phase E: ダッシュボード（Fastify API + htmx UI）

### E-1. Fastify APIルート
- [ ] `GET /api/summary` — 全体サマリー
- [ ] `GET /api/departments` — 部署一覧・状態
- [ ] `GET /api/departments/:name` — 部署詳細
- [ ] `GET /api/agents` — 担当者一覧・状態
- [ ] `GET /api/agents/:id` — 担当者詳細
- [ ] `GET /api/proposals` — 提案一覧
- [ ] `POST /api/proposals/:id/approve` — 提案承認
- [ ] `POST /api/proposals/:id/reject` — 提案差し戻し
- [ ] `GET /api/logs` — ログ・履歴
- [ ] `GET /api/kpi` — KPIダッシュボード
- [ ] `POST /api/control/stop` — 緊急停止
- [ ] `POST /api/control/pause/:department` — 部署一時停止
- [ ] `POST /api/control/policy` — 方針変更

### E-2. htmx UI
- [ ] レイアウトテンプレート（ヘッダー/サイドバー/メイン）
- [ ] 全体サマリーページ
  - 現在の運用テーマ・方針
  - 直近24時間/7日間の成果
  - 重要アラート
  - 次回ハートビート予定
- [ ] 部署別ページ
  - 実行中/完了/提案中の作業
  - 根拠データ
  - 停滞事項
- [ ] 担当者別ページ
  - 現在タスク/完了タスク
  - 生成した提案
  - ステータス
- [ ] 提案管理ページ
  - 提案一覧（フィルタ: 未確認/検討中/承認/差戻し）
  - 承認/差戻しボタン
  - 管理者コメント入力
- [ ] ログ・履歴ページ
  - ハートビート実行ログ
  - 投稿履歴
  - KPI推移グラフ
- [ ] 介入ページ
  - 投稿停止ボタン
  - 部署一時停止
  - 方針変更フォーム

### E-3. リアルタイム更新
- [ ] htmx の `hx-trigger="every 30s"` でポーリング
- [ ] ハートビート完了時のダッシュボード自動更新

**推定工数**: 5-7日
**依存**: Phase B（提案モデル）、Phase D（部署構造）

---

## Phase F: 安全設計（ガードレール、自動停止、縮退運転）

### F-1. ガードレール
- [ ] 価格変更幅の上限を設定ファイル化
- [ ] 禁止テーマの動的チェック強化
- [ ] 高リスク返信の自動検出精度向上
- [ ] 重複投稿検出の強化

### F-2. 自動停止条件
- [ ] 連続エラー回数による自動停止（consecutiveFailures閾値）
- [ ] 異常コスト検出による自動停止
- [ ] 異常エンゲージメント（炎上検知）による自動停止
- [ ] 管理者による緊急停止ボタン（ダッシュボード連携）

### F-3. 縮退運転
- [ ] トークン予算残量による段階的縮退
  - Level 1: 低優先度タスクをスキップ
  - Level 2: 軽量モデルへ切り替え
  - Level 3: 既存要約の再利用のみ
  - Level 4: 投稿停止、監視のみ
- [ ] フォールバック戦略の実装
  - API障害時の代替経路
  - 次回ハートビートへの先送りキュー

### F-4. 統一リトライポリシー
- [ ] 指数バックオフ付きリトライラッパー
- [ ] アダプタ層への統一適用

**推定工数**: 3-4日
**依存**: Phase B（予算管理）、Phase E（ダッシュボード）

---

## Phase G: テスト仕上げ

### G-1. ユニットテスト
- [ ] 提案フロー全体テスト
- [ ] 予算管理テスト
- [ ] メモリ階層テスト
- [ ] 差分処理テスト
- [ ] 縮退運転テスト

### G-2. 統合テスト
- [ ] 13ステップハートビートフロー全通しテスト
- [ ] 部署間連携テスト
- [ ] ダッシュボードAPI全ルートテスト

### G-3. E2Eテスト
- [ ] DryRunモードでのフルサイクルテスト
- [ ] ダッシュボードUIテスト

### G-4. 負荷・コストテスト
- [ ] トークン使用量のベンチマーク
- [ ] 予算制限下での動作確認
- [ ] 長時間運用シミュレーション

**推定工数**: 3-4日
**依存**: Phase A-F すべて

---

## タイムライン概要

| Phase | 内容 | 推定工数 | 依存 |
|-------|------|----------|------|
| A | 土台整備 | 2-3日 | なし |
| B | ドメインと状態 | 4-5日 | A |
| C | ハートビート強化 | 4-5日 | B |
| D | 各部署実装 | 5-7日 | C |
| E | ダッシュボード | 5-7日 | B, D |
| F | 安全設計 | 3-4日 | B, E |
| G | テスト仕上げ | 3-4日 | A-F |
| **合計** | | **26-35日** | |

---

## リスクと軽減策

| リスク | 軽減策 |
|--------|--------|
| ダッシュボードUI工数の膨張 | htmx で最小限のUI。SPAを避ける |
| メモリ階層の複雑化 | 最初はB層（部署別要約）とD層（ワーキングメモリ）に集中 |
| 予算管理の精度 | 初期はトークン概算。精緻化はPhase 3で |
| 部署分離によるコード膨張 | 既存サービスをラップする薄い層として実装 |
